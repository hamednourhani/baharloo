<?php global $hnmOpt; ?><?php get_header(); ?>    <div class="blog-page p82-topbot">        <div class="container">            <div class="row">                <div class="jst-cov"><h2 class="title-grp">                        <?php                        if ( is_category() ) {                            $category = get_category( get_query_var('cat') );                            printf( __( "<span>All posts in</span> %s", 'health-plus' ), single_cat_title( '', false ) );                        } elseif( is_tag() ) {                            printf( __( "<span>All posts tagged</span> %s", 'health-plus'), single_tag_title('', false) );                        } elseif ( is_day() ) {                            printf( __( "<span>Archive for</span> %s", 'health-plus' ), get_the_time( 'F j, Y' ) );                        } elseif ( is_month() ) {                            printf( __( "<span>Archive for</span> %s", 'health-plus' ), get_the_time( 'F, Y' ) );                        } elseif ( is_year() ) {                            printf( __( "<span>Archive for</span> %s", 'health-plus' ), get_the_time( 'Y' ) );                        } elseif ( is_author() ) {                            global $author; $userdata = get_userdata($author);                            printf( __( "<span>All posts by</span> %s", 'health-plus' ), $userdata->display_name );                        } else {                            _e( 'Archive', 'health-plus' );                        }                        ?>                    </h2></div>                <?php if( $hnmOpt['sidebar_pos'] == 'left' ) get_sidebar(); ?>                <div class="col-md-8 col-sm-7 left-section">                    <?php if ( have_posts() ) : while ( have_posts() ) : the_post(); ?>                        <?php get_template_part( 'content' ); ?>                    <?php endwhile; else: ?>                        <?php get_template_part( 'content', 'none' ); ?>                    <?php endif; ?>                    <?php get_template_part( 'content', 'pagination' ); ?>                </div>                <?php if( $hnmOpt['sidebar_pos'] == 'right' ) get_sidebar(); ?>            </div>        </div>    </div><?php get_footer(); ?>